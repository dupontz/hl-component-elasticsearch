test_metadata:
  type: config
  name: default config
  description: creates an ES cluster using the default config

security_group_rules:
  -
    from: 9200
    ip: 10.0.0.0/16
    desc: allow access to elasticsearch protocol